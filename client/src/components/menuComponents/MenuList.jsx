import Api from "../../api/menuApi"

function MenuList ({ categoryData }) {

  return (
    <>
      {/* {
        findId.map(index => (
            <div>
              <h4>{index.title}</h4>
            </div>
        ))
      } */}
    </>
  )
}

export default MenuList
